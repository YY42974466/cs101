#include <stdio.h>
#include <string.h>

int my_strlen(char* s){
    strlen(s);
    return strlen(s);
}
int main()
{
    char s[] = "UI is a good girl";
    printf("len = %d\n",my_strlen(s));
    return 0;
}
